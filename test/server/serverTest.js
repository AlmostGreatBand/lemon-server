'use strict';

const assert = require('assert');

assert.ok(true, "Must be true");

// TODO: Create some files hierarchy in this directory, test whatever can be tested.
// Tests are run by 'npm test' command (see package.json).